import Image from "next/image";
import Navbar from "@/components/Navbar";

export const metadata = {
    title: "Avery Chapman | Resume & CV",
};

export default function Resume() {
    return (
        <>
            <Navbar />
            <p>Content eventually (Resume)</p>
        </>
    );
}
